Palindrome Checker (Python)

This is a simple Python program to check whether a given word is a palindrome or not.
A palindrome is a word that reads the same forward and backward.
Example: madam, level, radar
